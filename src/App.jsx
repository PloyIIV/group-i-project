import Header from './components/Header.jsx'

function App() {
  return (
    <>
      {/* Header is kept in its own component so it can be reused on every page. */}
      <Header />
    </>
  )
}

export default App
